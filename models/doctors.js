module.exports = function (sequelize, DataTypes) {
  var Doctors = sequelize.define('Doctors', {
    name: {
      type: DataTypes.
    },
    location: {
      type: DataTypes.
    },
    })
  return Doctors
}
