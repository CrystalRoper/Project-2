module.exports = function (sequelize, DataTypes) {
  var Bps = sequelize.define('Bps', {
    date: {
      type: DataTypes.
    },
    systolic: {
      type: DataTypes.
    },
    diastolic: {
      type: DataTypes.
    },
    pulse: {
      type: DataTypes.
    }
  })
  return Bps
}
