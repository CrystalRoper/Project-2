module.exports = function (sequelize, DataTypes) {
  var Exercises = sequelize.define('Exercises', {
    date: {
      type: DataTypes.
    },
    type: {
      type: DataTypes.
    },
    duration: {
      type: DataTypes.
    }
  })
  return Exercises
}
